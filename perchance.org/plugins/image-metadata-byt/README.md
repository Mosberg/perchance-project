# image-metadata-byt

## Type
plugin

## Purpose
âœ­ âœ­ âœ­ âœ­ âœ­ âœ­ âœ­ âœ­ âœ­

## Files
- `image-metadata-byt.html`
- `image-metadata-byt.perchance`

## What It Does
- Provides plugin-level functionality inside `C:\Users\Rasmu\Documents\perchance.org-project-mosberg\mosberg-perchance.org\plugins\image-metadata-byt`.
- Is intended to be used together with the other modules in this repository.
- Exposes behavior through its `.perchance` implementation and optional `.html` companion page.

## Usage
Import with `{import:image-metadata-byt}` and evaluate it where needed in your generator/framework.

## Configuration
- Check the module's `.perchance` file for `settings`, ``, and exported functions/aliases.
- If this module is imported by wrappers/frameworks, configure it from the parent generator settings.

## Compatibility
- Built for Perchance module import patterns used in this repository.
- If dependencies are imported by name, ensure those imports are available in the same environment.

## Source Notes
- âœ­ âœ­ âœ­ âœ­ âœ­ âœ­ âœ­ âœ­ âœ­
- BYT  ðŸ‘„ðŸ†  âœ­ âœ­
- âœ­ âœ­ âœ­ âœ­ âœ­ âœ­ âœ­ âœ­ âœ­
- These functions are made to work with the https://perchance.org/text-to-image-plugin, and are useless for others images (I think).
- imgMeta = {import:image-metadata-byt}


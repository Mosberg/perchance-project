# corlist

## Type
plugin

## Purpose
Colors = {import:corlist}

## Files
- `corlist.html`
- `corlist.perchance`

## What It Does
- Provides plugin-level functionality inside `C:\Users\Rasmu\Documents\perchance.org-project-mosberg\mosberg-perchance.org\plugins\corlist`.
- Is intended to be used together with the other modules in this repository.
- Exposes behavior through its `.perchance` implementation and optional `.html` companion page.

## Usage
Import with `{import:corlist}` and evaluate it where needed in your generator/framework.

## Configuration
- Check the module's `.perchance` file for `settings`, ``, and exported functions/aliases.
- If this module is imported by wrappers/frameworks, configure it from the parent generator settings.

## Compatibility
- Built for Perchance module import patterns used in this repository.
- If dependencies are imported by name, ensure those imports are available in the same environment.

## Source Notes
- Colors = {import:corlist}
- This list was made for use in https://perchance.org/ai-art-generator-advanced.
- Anyone can use it, but please put credits on your perchance panel code (this list is not pure ctrl c+v, some research and filter was made.)
- ðŸ”½ Corlor list
- Mustard

